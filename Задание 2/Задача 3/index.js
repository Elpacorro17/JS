let index = 0; // Счётчик

// Список бегунов
let runners = ["Денис", "Виктория", "Антон", "Лена", "Никита"];

// Вывод с инкрементом
for (let runner of runners) {
    console.log(++index, runner);
}

